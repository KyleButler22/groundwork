/**
 * Placeholder for the Supabase-generated `Database` type.
 *
 * Once a Supabase project exists and the migrations in supabase/migrations/
 * have been applied to it, replace this whole file with the real thing:
 *
 *   npx supabase gen types typescript --project-id <ref> > src/types/database.ts
 *
 * (or `--local` against a local `supabase start`). Until then this stays
 * loose so src/lib/supabase.ts compiles meaningfully without claiming a
 * schema shape nobody has verified against a real database. Application
 * code should import from ./domain instead — this type only exists to
 * satisfy `createClient<Database>()`.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any
